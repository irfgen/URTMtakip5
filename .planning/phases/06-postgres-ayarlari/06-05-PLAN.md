---
phase: 06-postgres-ayarlari
plan: '05'
type: execute
wave: 4
depends_on:
  - '06-01'
  - '06-02'
  - '06-03'
  - '06-04'
files_modified:
  - backend/src/config/database.js
autonomous: false
requirements:
  - DB-07
  - DB-08

must_haves:
  truths:
    - "PostgreSQL connection can be established"
    - "All 56 models sync successfully"
    - "Migration system works with PostgreSQL"
  artifacts:
    - path: "backend/src/config/database.js"
      provides: "PostgreSQL connection configuration"
      min_lines: 234
  key_links:
    - from: "backend/src/config/database.js"
      to: "PostgreSQL server"
      via: "sequelize connection"
      pattern: "new Sequelize"
---

<objective>
Verify PostgreSQL connection and model sync works correctly with DB_DIALECT=postgresql.

Purpose: End-to-end verification of Phase 6 work
Output: Verified PostgreSQL compatibility
</objective>

<execution_context>
@/home/irfan/Belgeler/URTMtakip5/.claude/get-shit-done/workflows/execute-plan.md
@/home/irfan/Belgeler/URTMtakip5/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-postgres-ayarlari/06-CONTEXT.md
@.planning/phases/06-postgres-ayarlari/06-01-SUMMARY.md
@.planning/phases/06-postgres-ayarlari/06-02-SUMMARY.md
@.planning/phases/06-postgres-ayarlari/06-03-SUMMARY.md
@.planning/phases/06-postgres-ayarlari/06-04-SUMMARY.md
@backend/src/config/database.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify database.js dialectOptions</name>
  <files>backend/src/config/database.js</files>
  <action>
    Review database.js Phase 5 output for DB-07 requirements:

    1. dialectOptions for PostgreSQL (lines 31-33):
       - SSL configuration present for production
       - rejectUnauthorized set correctly

    2. Connection pool (lines 34-39, 56-61):
       - max: 20, min: 5, acquire: 30000, idle: 10000
       - Properly configured

    3. QueryTypes export (line 231):
       - QueryTypes exported for use by db-access.js
       - DB-08 requirement satisfied

    DB-07 and DB-08 are already implemented from Phase 5.
    Document and verify in summary.
  </action>
  <verify>
    <automated>
      echo "=== Verify dialectOptions ===" && \
      grep -A5 "dialect: 'postgres'" backend/src/config/database.js | head -10 && \
      echo "=== Verify QueryTypes export ===" && \
      grep "QueryTypes" backend/src/config/database.js | head -2
    </automated>
  </verify>
  <done>dialectOptions verified - SSL and pool configured</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: PostgreSQL connection test</name>
  <what-built>All Phase 6 changes ready for PostgreSQL testing</what-built>
  <how-to-verify>
    Prerequisites: PostgreSQL server must be running

    Test Steps:
    1. Set environment: export DB_DIALECT=postgresql
    2. Set DATABASE_URL or individual DB_* env vars
    3. Run: node -e "require('./backend/src/config/database').sequelize.authenticate().then(()=>console.log('OK')).catch(e=>console.error('FAIL:',e.message))"

    Expected: Connection successful message

    If PostgreSQL not available: This will be verified in Phase 8 (Test & Doğrulama)
  </how-to-verify>
  <resume-signal>Type "approved" or note if PostgreSQL not available in this runtime</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Document Phase 6 completion</name>
  <files>backend/src/config/database.js</files>
  <action>
    Summarize all Phase 6 verification results:

    DB-05 (56 models): Audited, no SQLite-specific code found
    DB-06 (PRAGMA removal): Models don't contain PRAGMA statements
    DB-07 (dialectOptions): Already configured in Phase 5
    DB-08 (QueryTypes): Exported from database.js, used correctly
    DB-09 (26 migrations): Audited, no SQLite-specific code found
    DB-10 (SQLite queries): No SQLite-specific raw SQL found
    DB-11 (Migration ordering): Verified correct timestamp ordering
    DB-12 (db-access.js): Verified compatible, no changes needed
    DB-13 (api-client.js): N/A - no database dependencies
    DB-14 (module-agent.js): N/A - uses db-access abstraction

    All requirements satisfied. Phase 6 complete.
  </action>
  <verify>
    <automated>echo "Phase 6 verification summary documented in 06-05-SUMMARY.md"</automated>
  </verify>
  <done>All DB-05 through DB-14 requirements documented</done>
</task>

</tasks>

<verification>
- DB_DIALECT=postgresql configured
- dialectOptions with SSL verified
- QueryTypes exported and used correctly
- No SQLite-specific code in models/migrations
</verification>

<success_criteria>
All 10 requirements (DB-05 through DB-14) verified complete. Phase 6 is done.
</success_criteria>

<output>
After completion, create .planning/phases/06-postgres-ayarlari/06-05-SUMMARY.md
</output>