---
phase: 06-postgres-ayarlari
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/ArizaBakim.js
  - backend/src/models/Bom.js
  - backend/src/models/Fason.js
  - backend/src/models/FasonGrup.js
  - backend/src/models/FasonIsEmri.js
  - backend/src/models/FasonTeklif.js
  - backend/src/models/Fatura.js
  - backend/src/models/FaturaKalem.js
  - backend/src/models/Firma.js
  - backend/src/models/Grup.js
autonomous: false
requirements:
  - DB-05
  - DB-06
  - DB-08
  - DB-10

must_haves:
  truths:
    - "All 56 model files are PostgreSQL compatible"
    - "No SQLite PRAGMA statements remain in models"
    - "QueryTypes usage is consistent and correct"
  artifacts:
    - path: "backend/src/models/"
      provides: "56 model files audited and updated for PostgreSQL"
      min_lines: 0
  key_links:
    - from: "backend/src/models/"
      to: "backend/src/config/database.js"
      via: "sequelize instance import"
      pattern: "require.*database.*sequelize"
---

<objective>
Audit and update all 56 model files for PostgreSQL compatibility. Remove SQLite-specific patterns, verify QueryTypes usage, fix any raw SQL with dialect-specific syntax.

Purpose: Ensure all Sequelize models work with PostgreSQL dialect
Output: Updated model files ready for PostgreSQL
</objective>

<execution_context>
@/home/irfan/Belgeler/URTMtakip5/.claude/get-shit-done/workflows/execute-plan.md
@/home/irfan/Belgeler/URTMtakip5/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-postgres-ayarlari/06-CONTEXT.md
@backend/src/config/database.js
@backend/src/models/ArizaBakim.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan models for SQLite-specific patterns</name>
  <files>backend/src/models/*.js</files>
  <action>
    Use grep to scan ALL 56 model files for SQLite-specific patterns:
    1. Search for "PRAGMA" statements
    2. Search for "sqlite" string references (case-insensitive)
    3. Search for "QueryTypes" usage patterns
    4. Search for raw SQL with backticks (`)
    5. Search for "LIKE '%" patterns that might be dialect-specific

    Create a fix script or list all files needing changes.

    Pattern checks:
    - grep -r "PRAGMA" backend/src/models/
    - grep -ri "sqlite" backend/src/models/
    - grep -r "QueryTypes" backend/src/models/
    - grep -r "\`" backend/src/models/ | grep -v "backtick"
  </action>
  <verify>
    <automated>
      echo "=== SQLite Pattern Check ===" && \
      grep -r "PRAGMA" backend/src/models/ 2>/dev/null || echo "No PRAGMA found" && \
      echo "=== QueryTypes check ===" && \
      grep -r "QueryTypes" backend/src/models/ 2>/dev/null | head -5 || echo "No QueryTypes found"
    </automated>
  </verify>
  <done>Report generated listing all files with issues</done>
</task>

<task type="auto">
  <name>Task 2: Fix model files with issues</name>
  <files>backend/src/models/*.js</files>
  <action>
    Based on scan results from Task 1, update each affected model file:

    1. Remove any PRAGMA statements if found
    2. Remove any hardcoded 'sqlite' dialect checks
    3. For QueryTypes usage - ensure it's imported from database.js, not used directly in model files
    4. Fix any raw SQL backtick syntax to use ? replacements
    5. For JSON fields - verify they use DataTypes.JSONB for PostgreSQL

    Common fixes:
    - Replace: sequelize.query("PRAGMA...") → remove
    - Replace: storage: ':memory:' or storage path references → remove (handled by database.js)
    - Replace: QueryTypes direct usage in model files → move to db-access.js pattern
  </action>
  <verify>
    <automated>
      echo "=== Verify no PRAGMA ===" && \
      (grep -r "PRAGMA" backend/src/models/ 2>/dev/null && echo "FAIL: PRAGMA still present" || echo "PASS: No PRAGMA") && \
      echo "=== Verify no hardcoded sqlite ===" && \
      (grep -ri "sqlite" backend/src/models/ 2>/dev/null && echo "WARN: sqlite references" || echo "PASS: No sqlite refs")
    </automated>
  </verify>
  <done>All model files updated, grep checks pass</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify model associations</name>
  <what-built>Updated model files with foreign key relationships</what-built>
  <how-to-verify>
    1. Check that all foreignKey references match actual column names
    2. Verify belongsTo/hasMany associations are correct
    3. Run: node -e "require('./backend/src/config/database').sequelize.models" to list all models
    4. Verify model count shows 56 models loaded
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- 56 model files exist in backend/src/models/
- No SQLite PRAGMA statements in any model
- No hardcoded 'sqlite' dialect references
- All models load without errors via `sequelize.models`
</verification>

<success_criteria>
All 56 model files are verified PostgreSQL-compatible with proper associations and no SQLite-specific code.
</success_criteria>

<output>
After completion, create .planning/phases/06-postgres-ayarlari/06-01-SUMMARY.md
</output>