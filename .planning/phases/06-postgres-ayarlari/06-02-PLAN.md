---
phase: 06-postgres-ayarlari
plan: '02'
type: execute
wave: 2
depends_on:
  - '06-01'
files_modified:
  - backend/src/migrations/*.js
autonomous: false
requirements:
  - DB-09
  - DB-10
  - DB-11

must_haves:
  truths:
    - "All 26 migration files are PostgreSQL compatible"
    - "Migration ordering respects dependencies"
    - "No SQLite-specific SQL syntax remains"
  artifacts:
    - path: "backend/src/migrations/"
      provides: "26 migration files audited for PostgreSQL"
      min_lines: 0
  key_links:
    - from: "backend/src/migrations/"
      to: "backend/src/config/database.js"
      via: "runMigrations() function"
      pattern: "require.*database"
---

<objective>
Audit and update all 26 migration files for PostgreSQL compatibility. Fix SQLite-specific syntax, verify ENUM handling, ensure proper migration ordering.

Purpose: Ensure all migrations run correctly on PostgreSQL
Output: Updated migration files with proper ordering
</objective>

<execution_context>
@/home/irfan/Belgeler/URTMtakip5/.claude/get-shit-done/workflows/execute-plan.md
@/home/irfan/Belgeler/URTMtakip5/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-postgres-ayarlari/06-CONTEXT.md
@.planning/phases/06-postgres-ayarlari/06-01-SUMMARY.md
@backend/src/config/database.js
@backend/src/migrations/20250101120000-create-satislar-table.js
@backend/src/migrations/20260128000001-create-uygunsuzluk-tables.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan migrations for PostgreSQL issues</name>
  <files>backend/src/migrations/*.js</files>
  <action>
    Scan all 26 migration files for SQLite-specific patterns:

    1. Search for raw SQL with SQLite syntax:
       - grep -r "PRAGMA" backend/src/migrations/
       - grep -r "sqlite_master" backend/src/migrations/
       - grep -r "journal_mode" backend/src/migrations/

    2. Check for ENUM compatibility:
       - Verify all ENUM definitions use PostgreSQL-compatible syntax
       - PostgreSQL ENUMs need to be created separately or use array syntax

    3. Check for autoIncrement vs SERIAL:
       - queryInterface.createTable should use INTEGER with autoIncrement: true
       - This maps to SERIAL in PostgreSQL

    4. Verify TIMESTAMP defaults:
       - Sequelize.literal('CURRENT_TIMESTAMP') works in both
       - But check for SQLite-specific functions

    5. Check JSON handling:
       - JSON type in migrations may need JSONB for PostgreSQL
  </action>
  <verify>
    <automated>
      echo "=== SQLite-specific patterns ===" && \
      (grep -r "PRAGMA\|sqlite_master\|journal_mode" backend/src/migrations/ 2>/dev/null || echo "None found") && \
      echo "=== Migration count ===" && \
      ls backend/src/migrations/*.js 2>/dev/null | wc -l
    </automated>
  </verify>
  <done>Issue report generated for all migrations</done>
</task>

<task type="auto">
  <name>Task 2: Fix migration files with issues</name>
  <files>backend/src/migrations/*.js</files>
  <action>
    Based on scan results, update affected migration files:

    1. Remove any PRAGMA, sqlite_master references
    2. For ENUM types - PostgreSQL requires explicit type definition
       - Option A: Create ENUM type separately via queryInterface.sequelize.query
       - Option B: Use STRING with validate enum in model (but model not in migration)
       - Recommendation: Use STRING for now, let model enforce

    3. For JSON fields - use DataTypes.JSON instead of JSONB in migrations
       - Let PostgreSQL handle as JSON type

    4. Verify all foreign key constraints use proper CASCADE/SET NULL

    5. Check migration file naming for proper ordering:
       - Files should be named: YYYYMMDDHHMMSS-description.js
       - Disable files end with .disabled
  </action>
  <verify>
    <automated>
      echo "=== Re-scan for SQLite patterns ===" && \
      (grep -r "PRAGMA\|sqlite_master\|journal_mode" backend/src/migrations/ 2>/dev/null && echo "FAIL" || echo "PASS: Clean") && \
      echo "=== Check for .disabled files ===" && \
      ls backend/src/migrations/*.disabled 2>/dev/null || echo "No disabled files"
    </automated>
  </verify>
  <done>All migrations updated and verified</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify migration ordering</name>
  <what-built>Migration files with proper dependency ordering</what-built>
  <how-to-verify>
    1. List all migration files in order
    2. Verify no migration references a table created in a later migration
    3. Check foreign key dependencies - tables referenced must be created first
    4. Run: node backend/src/config/database.js migrate (dry run check)

    Key dependencies to verify:
    - uygunsuzluk_raporlari needs personel, tezgahlar tables
    - satislar needs makinalar table
    - uygunsuzluk_* tables need uygunsuzluk_raporlari
  </how-to-verify>
  <resume-signal>Type "approved" or describe ordering issues</resume-signal>
</task>

</tasks>

<verification>
- 26 migration files exist (some may be .disabled)
- No SQLite-specific patterns found
- Migration files properly ordered by timestamp
- No circular dependencies
</verification>

<success_criteria>
All 26 migration files are PostgreSQL compatible with proper ordering.
</success_criteria>

<output>
After completion, create .planning/phases/06-postgres-ayarlari/06-02-SUMMARY.md
</output>