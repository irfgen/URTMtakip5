---
phase: 08-test-ve-dogrulama
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/postgres.test.js
  - backend/scripts/test-postgresql.js
  - backend/scripts/test-feature-flag.js
autonomous: false
requirements:
  - DB-19
  - DB-20
  - DB-21
  - DB-22
  - DB-23
  - DB-24
  - DB-25

must_haves:
  truths:
    - "Unit tests run with PostgreSQL dialect"
    - "Integration tests pass with PostgreSQL"
    - "Feature flag DB_DIALECT switching works"
    - "Rollback mechanism validated"
  artifacts:
    - path: "backend/tests/postgres.test.js"
      provides: "Unit/integration tests for PostgreSQL"
    - path: "backend/scripts/test-postgresql.js"
      provides: "PostgreSQL connection and query tests"
    - path: "backend/scripts/test-feature-flag.js"
      provides: "DB_DIALECT feature flag validation"
  key_links:
    - from: "backend/src/config/database.js"
      to: "PostgreSQL tests"
      via: "DB_DIALECT=postgresql"
---

<objective>
Validate PostgreSQL migration with tests and feature flag verification.
</objective>

<execution_context>
@/home/irfan/Belgeler/URTMtakip5/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@backend/src/config/database.js
@backend/MIGRATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostgreSQL test script</name>
  <files>backend/tests/postgres.test.js</files>
  <action>
Create test script that validates PostgreSQL connectivity and query functionality:
1. Tests PostgreSQL connection via sequelize.authenticate()
2. Tests basic CRUD operations on a simple table
3. Tests transaction support
4. Tests QueryTypes raw queries
5. Tests batch insert performance
6. Uses DB_DIALECT=postgresql environment variable

This is a smoke test, not full unit tests — validates the migration works.
  </action>
  <verify>
DB_DIALECT=postgresql node backend/tests/postgres.test.js --help 2>&1 || echo "PostgreSQL not available"
</verify>
<done>PostgreSQL test script created</done>
</task>

<task type="auto">
  <name>Task 2: Create feature flag test</name>
  <files>backend/scripts/test-feature-flag.js</files>
  <action>
Create feature flag test that validates DB_DIALECT switching:
1. Test with DB_DIALECT=sqlite - verify SQLite connects
2. Test with DB_DIALECT=postgresql - verify PostgreSQL connects
3. Test switching dialects in same process (if possible)
4. Verify model loading works under both dialects
5. Document expected behavior

This validates the rollback mechanism and dual-database support.
  </action>
  <verify>
node backend/scripts/test-feature-flag.js --help 2>&1
</verify>
<done>Feature flag test created</done>
</task>

<task type="auto">
  <name>Task 3: Create migration verification script</name>
  <files>backend/scripts/test-migration.js</files>
  <action>
Create script that validates data integrity after migration:
1. Connects to both SQLite (source) and PostgreSQL (target)
2. Compares row counts for all tables
3. Validates primary key integrity
4. Checks data types are correct in PostgreSQL
5. Reports any discrepancies

This is the final validation before switching to PostgreSQL.
  </action>
  <verify>
node backend/scripts/test-migration.js --help 2>&1
</verify>
<done>Migration verification script created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Three validation scripts for PostgreSQL migration testing</what-built>
  <how-to-verify>
1. Run: node backend/tests/postgres.test.js --help
   - Should show test options
2. Run: node backend/scripts/test-feature-flag.js --help
   - Should show feature flag test options
3. Review code: ensure tests check PostgreSQL-specific behavior
4. Run with PostgreSQL running to validate actual migration
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Document test results</name>
  <files>backend/TESTING.md</files>
  <action>
Create TESTING.md documenting:
1. How to run PostgreSQL tests
2. How to validate feature flag switching
3. How to verify migration integrity
4. Expected results and pass criteria
5. Troubleshooting common issues
</action>
  <verify>File exists with documented test procedures</verify>
  <done>TESTING.md created with complete documentation</done>
</task>

</tasks>

<verification>
- postgres.test.js exists and shows help
- test-feature-flag.js exists and shows help
- test-migration.js exists and shows help
- TESTING.md documents all test procedures
</verification>

<success_criteria>
1. postgres.test.js created and runnable
2. test-feature-flag.js created and runnable
3. test-migration.js created and runnable
4. TESTING.md documents full test process
5. Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-ve-dogrulama/08-01-SUMMARY.md`
</output>