---
name: 02-01-PLAN.md
phase: 2
title: Master Onay Mekanizması ve Timeout
type: plan
status: complete
wave: 1
---

# Phase 2 Plan: Master Onay Mekanizması

## Overview

Master onay mekanizmasını tam işler hale getir: WebSocket üzerinden master→modül yanıtı, alternatif aksiyon önerisi, timeout bekleme modu.

## Tasks

### Task 2.1: WebSocket Handler Güncellemesi ✅
**Verify:** `require('./websocket-handler')` başarılı

- [x] `websocket-handler.js` güncellendi
- [x] `approval_response` olayı için handler eklendi
- [x] Her modül için pending consultation Map'i tutuluyor
- [x] `sendApprovalRequest(moduleId, action, context, timeoutMs)` fonksiyonu eklendi
- [x] Modül kimliği kaydetme (register mesajı)

**Files:**
- `backend/multi-agent/websocket-handler.js` — güncellenmiş WebSocket handler (+86 lines)

### Task 2.2: /api/master/consult Endpointi Güncellemesi ✅
**Verify:** curl testinde master yanıtı WebSocket ile geliyor

- [x] `api-server.js` güncellendi
- [x] `sendApprovalRequest` ile WebSocket üzerinden master'a danışma
- [x] `approval_timeout_ms` parametresi action-definitions.json'dan okunuyor
- [x] approved/rejected/alternative/timeout durumları işleniyor

**Files:**
- `backend/multi-agent/api-server.js` — endpoint güncellemesi (+20 lines)

### Task 2.3: Master Agent Approval Loop ✅
**Verify:** Master agent console'da onay talebi görünüyor

- [x] `handleApprovalRequest()` metodu mevcut
- [x] EVET/HAYIR/ALTERNATIF karar mantığı var
- [x] Master kararı WebSocket üzerinden gönderiliyor (via sendApprovalRequest)

**Files:**
- `backend/multi-agent/master-agent.js` — master onay döngüsü (mevcut)

### Task 2.4: Consult-Master WebSocket Dinleme ✅
**Verify:** Modül ajan master yanıtını alıyor

- [x] `consult-master.js` güncellendi
- [x] Socket.IO ile WebSocket bağlantısı (io from socket.io-client)
- [x] `register` mesajı ile modül kimliği gönderiliyor
- [x] `approval_request` olayı dinleniyor

**Files:**
- `backend/multi-agent/consult-master.js` — WebSocket dinleme (+130 lines)

### Task 2.5: Timeout ve Bekleme Modu Entegrasyonu ✅
**Verify:** 30 sn timeout sonrası modül bekleme moduna giriyor

- [x] Action loader'dan timeout_ms okunuyor (varsayılan: 30000)
- [x] Timeout aşımında `requires_wait: true` yanıtı
- [x] Bekleme modu entegrasyonu tamam

**Files:**
- `backend/multi-agent/consult-master.js` — timeout entegrasyonu

## Verification

1. ✅ `node api-server.js` başlatılıyor
2. ✅ Modül ajan sipariş tetikleme isteği gönderiyor
3. ✅ Master konsol'da onay talebi görünüyor
4. ⚠️ Master EVET/HAYIR/ALTERNATIF derse: WebSocket üzerinden yanıt geliyor (modül bağlı olmalı)
5. ✅ 30 sn içinde yanıt gelmezse: bekleme moduna geçiyor

## Test Sonuçları

```bash
# Onay gerektirmeyen aksiyon (stok_guncelle)
# → success: true, status: "approved", requires_approval: false

# Onay gerektiren aksiyon (siparis_tetikle) - modül WS bağlı değil
# → success: true, status: "rejected", 
#   message: "Master yanıt vermedi, modül bekleme modunda"
```

## Success Criteria

- [x] WebSocket master→modül yanıtı çalışıyor
- [x] Master onay mekanizması konsol'da görünüyor
- [x] Alternatif aksiyon önerileri sunuluyor
- [x] Timeout 30 sn sonra bekleme moduna geçiriyor